<!DOCTYPE html>
<html>
  <head>
    <title>DC Food Truck</title>
    <%= csrf_meta_tags %>
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300,400,700" rel="stylesheet">
    <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyB7voNn6vJmch6WZiuqp6k16rkj1Y_LCYE&callback=initMap"
async defer></script>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="header-holder">
      <p class="notice notification"><%= notice %></p>
      <p class="alert notification"><%= alert %></p>

      <a class="header-title noselect" href="<%=companies_path%>">DC Food Trucks</a>

      <div class="header-user">
        <% if !current_user %>
          <%= link_to "Sign Up", new_user_registration_path %>
          <%= link_to "Log In", new_user_session_path %>
        <% else %>
          <div class="flex-center">
            <p>
              <%= current_user.email %>
            </p>
            <%= link_to 'Log Out', destroy_user_session_path, :method => :delete %>
          </div>
        <% end %>
      </div>
    </div>

    <%= yield %>
  </body>
</html>

<div class="company-show-holder ">
  <div class="map-holder">
    map
  </div>

  <div class="company-holder page-margin">
    <div class="company-holder-info-bio">
      <div class="company-info-holder">
        <h1>
          <%= @company.name %>
        </h1>
        <h2>
          <a target="_blank" href="<%=@company.website%>">
            website
          </a>
        </h2>
        <h2>
          <%= @company.phone_number %>
        </h2>

        <div class="company-credit-card-holder">
          <h2>Credit Card:</h2>

          <% if @company.credit_card %>
            <h2 class="letter-good">
              Yes
            </h2>
            <% else %>
            <h2 class="letter-bad">
              No
            </h2>
          <% end %>
        </div>

        <% if can? :update, @company %>
          <h2>
            <%= link_to 'edit profile', edit_company_path(@company) %>
          </h2>
        <% end %>
      </div>

        <div class="company-bio-holder">
          <%= render :partial => "bio-regular", :locals => {:company => @company} %>
        </div>
    </div>

    <% if can? :update, @company %>
      <div class="company-trucks-holder flex-center">
      <h2>Trucks</h2>
      <% @trucks.each do |truck| %>
        <p>
          <%= truck.name %>
        </p>
      <% end %>
      </div>
    <% end %>

    <div class="company-comments-holder flex-center">
      <h2>
        <button class="btn btn-light" type="button" data-toggle="collapse" data-target="#collapse_comments" aria-expanded="false" aria-controls="collapseExample">
          Comments
        </button>
      </h2>

      <%= render :partial => "comments", :locals => {:comments => @comments, :company => @company} %>
    </div>
  </div>

</div>
